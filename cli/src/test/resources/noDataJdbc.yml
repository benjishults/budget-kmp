persistence:
  type: JDBC
  jdbc:
    driver: org.postgresql.Driver
    schema: clean_after_test
    user: test
    password: test
    budgetName: clean after test
    host: localhost
  file:
    dataDirectory: src/test/resources/noDataJdbc
